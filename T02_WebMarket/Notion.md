###  Попытка 1 - отклонено
Мой ответ: [GIT](https://github.com/Maybeprogramming/CSharpCleanCode/blob/a68a3d3efcb7e429fada299f19ce61668d362e71/T02_WebMarket/Program.cs)

Оценено в	пятница, 24 октября 2025, 12:52 **Денис Калужин**

Отзыв в виде комментария

Здравствуйте

1. Goods - это товары, но Good - это хорошо. Другое слово надо.
2. Корзина и заказ - это разные классы. 
3. public Order Order(),  public Cart Cart() - во-первых, методы именуем глаголами, во-вторых, не используйте один термин в двух значениях, это путает.
4. Не забываем про предыдущие уроки. Везде должны быть конструкторы и валидация входных параметров с необходимыми исключениями.
5. Для проверки строк лучше использовать не просто сравнение с null, а метод IsNullOrWhiteSpace
https://learn.microsoft.com/ru-ru/dotnet/api/system.string.isnullorwhitespace?view=net-8.0
6. ~~~csharp
   public void Remove(int amount)
   {
       if (amount < 0) - тут надо проверять еще, что у нас есть достаточно товара.
       //...
   }
   ~~~
7. ~~~csharp
   TryRemoveGoods(IEnumerable<IReadOnlyCell> cellsOrdered) 
   ~~~ 
   тоже
8. Тут не надо через события работать. Мы не отдаем заказ, если на складе что-то пошло не так, а у вас это 2 независимых действия. 
9. Корзина должна иметь ссылку на склад, чтоб проверять там наличие товара, но не полноценную, а по урезанному интерфейсу, который позволит только проверять наличие, бронировать и удалять товар.
По сути, объединяем склад и GoodsValidator. 
10. В напильнике мы забываем про мягкую обработку некорректного поведения. Тут все на исключениях. 
11. ~~~csharp 
    throw new NullReferenceException(nameof(cell)); 
    ~~~
    это исключение для входных параметров. Если нул в процессе работы появляется, то это уже невалидная операция или просто  ArgumentException если выброс . 
12. ~~~csharp
    public void Delive(Goods good, int amount)
    ~~~
    тут бы я новую ячейку создавал, только если такой еще нет. Для проверки наличия товара и добавления кол-ва не нужна новая ячейка. Не мусорьте в памяти.

---
###  Попытка 2 - отклонено
Мой ответ: [GIT](https://github.com/Maybeprogramming/CSharpCleanCode/blob/b9f79aef62afe2850d11136c763e0a08f9db0f69/T02_WebMarket/Program.cs)

Оценено в	понедельник, 27 октября 2025, 13:04 - Денис Калужин

Отзыв в виде комментария	

Здравствуйте

1. Не исправлено. Goods - это товары, но Good - это хорошо. Другое слово надо.
2. public Cart GetCart() => _cart; - на каждый запрос создаем новую корзину. 
И в сокращенных метода (в отличие от свойств) все что после лямбды с новой строки пишем
3. TryGetGoodsByName - от методов с таким именем ожидается возвращение bool, по аналогии с TryParse.
4. IReadOnlyCell cellInStock = _cells.First(cell => cell.Goods.Name == cellToVerify.Goods.Name);
            if (cellInStock != null)
                return cellInStock;
            return null; - все вот это можно записать в одну строку 
 return _cells.First(cell => cell.Goods.Name == cellToVerify.Goods.Name);
5. И проверка из TryGetGoodsByName дублируется в TryRemoveOrderedGoods
6. _order = new Order();- не должен быть полем. Он создается в момент запроса.
7. Utils.Print($"Ошибка - нет нужного количества товара на складе"); - кидаем исключение.
8. if (_warehouse.IsGoodsAvaiableByAmount(cell) == false) - так по одному можно бесконечно много товаров добавить. Учитывайте, сколько их уже есть в корзине.


---
###  Попытка 3 - принято
Мой ответ: [GIT](https://github.com/Maybeprogramming/CSharpCleanCode/blob/34673b31fe4d96ea3138ec252f11e309a7a97e97/T02_WebMarket/Program.cs)

Оценено в	понедельник, 27 октября 2025, 18:18 - Николай Титаренко

Отзыв в виде комментария

1. Cart.Add - если в тележку запрашивается добавление товара, следует проверять наличие товара с учётом того что уже находится в тележке чтобы проверка не позволяла положить в корзину ещё 10 товаров, когда в ней уже таких товаров записано 9, а на складе всего 11